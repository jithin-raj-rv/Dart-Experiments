# hello_world

## Description

A classic Flutter "Hello World" application with a counter feature. The app displays a configurable title ("Bulu Bulu") and includes an interactive counter that increments when the floating action button is pressed.

## Setup

```bash
flutter pub get
```

## Run

```bash
flutter run