# new_project (Basic Counter)

## Description

A simple Flutter counter application built to demonstrate the MVVM architecture pattern. The project separates the counter logic into a dedicated `Counter` class within the `counter/` directory, keeping the UI layer clean and passive.

## Setup

```bash
flutter pub get
```

## Run

```bash
flutter run