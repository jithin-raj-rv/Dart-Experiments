# flutter_application_1 (Local Notifications)

## Description

A Flutter project exploring local notification functionality. This app demonstrates scheduled notifications using `flutter_local_notifications` and `awesome_notifications`, with timezone-aware scheduling via `flutter_timezone` and `timezone` packages, and permission handling with `permission_handler`.

## Setup

```bash
flutter pub get
```

## Run

```bash
flutter run